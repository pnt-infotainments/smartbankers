<%= form_for([:administrator, @post_type]) do |f| %>
  <% if @post_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_type.errors.count, "error") %> prohibited this post_type from being saved:</h2>

      <ul>
      <% @post_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
